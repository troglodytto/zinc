(func something (arg arg arg) (
    (if (cond) (
        (set X 43)
    ) else (
        (set X 42)
    ))

    (ret 12)
))


(func main (arg arg) (
    ; This is a comment
    (something (arg, arg, arg))
    (else2342klasd (arg, arg, arg))
    
    (+ "\n" "Multiple-strings" 'single-quote-string')
    (!= f 4)
    (set X 12)
    
    (set array { "arrays", "are", "defined", "like", "this" })
    
    (print x)
    
    (+ (13, 3))
))
